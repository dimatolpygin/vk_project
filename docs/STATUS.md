# STATUS — текущее состояние проекта

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-08-08 (этап 9 задеплоен, ждёт UAT)
**Текущий этап**: Этап 9 — Раздел «Тренды», фото-часть (🚧 в проде, ждёт живого прогона)
**Следующий шаг**: заказчик прогоняет этап 9 в боте по чекбоксам в [`ROADMAP.md`](ROADMAP.md) и говорит `stage-9-done`. Открыты два вопроса к нему: вёрстка главного меню (сведённые в ряд «Свой промт» и «Изменить фото» — мой выбор, не его) и эмодзи в названиях узлов раздела. Параллельно висит хвост батча 1: этапы 1–3 задеплоены, но не прогнаны через UAT — чекбоксы в [`ROADMAP.md`](ROADMAP.md) ждут живой проверки заказчиком.

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 1 | Раздел «Примеры работ» | 🚧 | `stage-1-done` | — | — |
| 2 | Кнопка «Сделать такие фото» в рассылках | 🚧 | `stage-2-done` | — | — |
| 3 | Автодогон через 3 минуты после тарифов | 🚧 | `stage-3-done` | — | — |
| 4 | Вёрстка, тексты тарифов и «Мой профиль» | ✅ | `stage-4-done` | `9df83bc` | 2026-08-07 |
| 5 | Дерево разделов + управление из админки | ✅ | `stage-5-done` | `7ca2e75` | 2026-08-07 |
| 6 | Подменю «Парное/Семейное фото» | ✅ | `stage-6-done` | `6810f92` | 2026-08-08 |
| 7 | Раздел «Детские фото» | ✅ | `stage-7-done` | `ff8c5dc` | 2026-08-08 |
| 8 | Раздел «Поздравления» | ✅ | `stage-8-done` | `1d2f97c` | 2026-08-08 |
| 9 | Раздел «Тренды» (фото-часть) | 🚧 | `stage-9-done` | — | — |
| 10 | Видео-генерация (Seedance) | ⏸ | `stage-10-done` | — | — |

Этапы 1–3 реализованы в коде (коммиты `ead29b6`, `deba660`, `cd99bea`) и задеплоены,
но **не закрыты**: acceptance-критерии требуют живой проверки в боте. Теги не ставятся
до UAT. Этапы 4–10 — по `542_ТЗ.md`, от батча 1 не зависят.

Детальные критерии приёмки каждого этапа — в [`ROADMAP.md`](ROADMAP.md).

## Активная работа

**Этап**: 9 — Раздел «Тренды», фото-часть.
**Ветка**: `main`
**Сводка задач**: сделано и задеплоено — коммит `313039a`, миграция 33 применена 10:11:36 UTC, ошибок в логах 0. В проде четыре узла раздела `trends`, виден один («Фото тренды»), три видео-узла выключены. Осталcя живой прогон. Батч 1 (этапы 1–3) в проде и ждёт UAT.
**Как верифицируем**: живой прогон в ВК по чекбоксам этапа.

## Известные блокеры

- **Этап 10 (видео)**: нет документации по видео-модели и не выбрана схема генерации —
  прямой image-to-video через Seedance или коллаж ракурсов через Nano Banana.
  Что нужно для разблокировки: заказчик передаёт доку по модели и выбирает схему.
  Также нужны параметры видеотарифа (цена, количество генераций) и базовая
  стоимость видео-промта в генерациях.
- **Главное меню упёрлось в лимит ВК** (влияет на этап 9): шесть кнопок при лимите
  в шесть строк. Седьмая кнопка («Тренды») заставит `fitInlineRows` склеивать
  кнопки по две в ряд. Тест `TestMainMenuKeepsEverySectionOnItsOwnRow` падает
  в этот момент. Решение принимает заказчик: две кнопки в ряд или часть разделов
  под общей кнопкой.
- **Нет контента для новых разделов** (этапы 7–9): тексты промтов, картинки экранов
  и список праздников для «Детского режима», «Трендов» и «Поздравлений».
  Этапы этим **не блокируются**: разделы включаются сразу, пустые узлы показывают
  экран-заглушку `section_soon` («Скоро») — она сделана в этапе 6 и уже в проде.
  Что нужно для снятия заглушек:
  заказчик наполняет узлы промтами и картинками из админки, релиз не требуется.
- **Нет контента для примеров работ** (этап 1): 6 экранов категорий отдают текст-заглушку
  без картинки. Что нужно для разблокировки: заказчик загружает коллажи в админке
  (раздел «Сообщения», экраны `examples_self`, `examples_couple`, `examples_kids`,
  `examples_edit`, `examples_greetings`, `examples_misc`) и правит тексты под себя.

## История закрытий

Записывать в обратном хронологическом порядке. Формат:
`YYYY-MM-DD — Этап N: <название> — tag stage-N-done — короткая заметка с конкретикой верификации`.

2026-08-08 — Этап 8: Раздел «Поздравления» — tag `stage-8-done` — коммит `1d2f97c`,
миграция 00032, контейнеры пересозданы 09:48:06 UTC, ошибок в логах после релиза 0.
Заказчик прогнал живой прогон и принял. Нетривиальные решения:
(1) заказчик выбрал вариант Б — три уровня (адресат → праздник → промты);
пол задаёт адресат через `prompt_gender` и наследует празднику, поэтому один
и тот же «День рождения» под разными адресатами отдаёт разные промты, а пол
пользователя не участвует вовсе;
(2) обратная сторона этого решения: промт висит на празднике **внутри** адресата,
и общий для обоих текст приходится заводить дважды — сказано заказчику явно;
(3) праздники заведены стартовым набором (день рождения и Новый год обоим,
23 февраля мужчинам, 8 марта женщинам) — чтобы структура была видна и её было
с чего править из админки;
(4) главное меню упёрлось в лимит ВК: шесть кнопок при шести строках. Добавлен
тест `TestMainMenuKeepsEverySectionOnItsOwnRow`, который упадёт на седьмой кнопке
в этапе 9 — момент склейки кнопок по две в ряд будет пойман до пользователя;
(5) миграция снова проверена на боевой БД в откатываемой транзакции; заодно
подтвердилось, что заказчик уже добавил «Гендер-пати» в детский раздел
и прогнал детскую генерацию — управление деревом из админки работает.

2026-08-08 — Этап 7: Раздел «Детские фото» — tag `stage-7-done` — коммит `ff8c5dc`,
миграция 00031, контейнеры пересозданы 09:26:53 UTC, ошибок в логах после релиза 0.
Заказчик прогнал живой прогон и принял. Нетривиальные решения:
(1) «Мальчик» и «Девочка» — два **корня** раздела с `prompt_gender` male/female:
пол промтов берётся с узла, поэтому экран выбора пола пользователя здесь не нужен
вовсе (женщина, выбравшая «Мальчик», получает мужские детские промты);
(2) «Запомнить фото» в детском разделе намеренно отключено — там хранится фото
взрослого, а нужно фото ребёнка; поведение зафиксировано отдельным тестом;
(3) выход из дерева разделов перестал быть перечислением шагов в `switch` —
шаг → раздел ищется по индексу из `sectionSpecs`, иначе каждый следующий раздел
означал бы правку в двух местах; этапам 8–9 хватит одной записи в карте;
(4) тип генерации `kids` добавлен в CHECK таблицы `generations` — до релиза
проверено, что все пять существующих типов новый CHECK проходят;
(5) сохранённая раскладка `main_menu` перед сбросом сверена с кодовой: они
совпадали побайтово, так что правки заказчика при сбросе не пострадали.

2026-08-08 — Этап 6: Подменю «Парное/Семейное фото» — tag `stage-6-done` — коммит
`6810f92`, миграция 00030, контейнеры пересозданы 10:52:41 UTC, миграция применена
10:52:52, ошибок в логах после релиза 0. Заказчик прогнал живой прогон и принял.
Нетривиальные решения:
(1) три узла заведены **корнями раздела** `couple`, а не детьми одного технического
корня, как было записано в ROADMAP: навигатор этапа 5 и так показывает корни раздела
сразу после загрузки фото, технический корень означал бы лишний тап у пользователя;
(2) прежний экран `couple_categories` не выброшен, а привязан к узлу «Парное фото»
через `screen_key` — текст «Теперь выбери категорию» остался ровно на своём уровне,
а верхний уровень получил новый `couple_submenu`;
(3) общий экран-заглушка `section_soon` поднят из этапа 7 в шестой — его требует
чекбокс приёмки «пустой подраздел показывает понятный экран»; заодно на этом экране
теперь пишется состояние, без него «Назад» уходил в главное меню мимо подменю;
(4) миграция снова проверена на боевой БД в откатываемой транзакции: 3 корня,
8 детей под «Парным фото», 45 промтов в дереве раздела — ни один не потерян;
(5) `couple.go` не тронут вовсе — весь этап уложился в миграцию, две строки
в `sectionSpecs` и ветку пустых промтов, что и было целью фундамента из этапа 5.

2026-08-07 — Этап 5: Дерево разделов и управление им из админки — tag `stage-5-done` — коммит
`7ca2e75`, миграция 00029, контейнер пересоздан 10:08:55 UTC, ошибок в логах после релиза 0.
Заказчик прогнал живой прогон и принял. Нетривиальные решения:
(1) миграция проверялась на боевой БД в откатываемой транзакции до релиза — наборы категорий
до и после совпали (9 в «Фото для себя» для мужчины, 8 парных), поэтому регресс был известен
заранее, а не по факту;
(2) «раздел» перестал подделываться колонкой `gender` — у парных категорий теперь
`section = 'couple'` и `prompt_gender = 'couple'`, последняя колонка и есть флаг «пол задаётся
кнопкой подменю» из ТЗ и наследуется вниз по дереву (понадобится Поздравлениям);
(3) старые callback-типы `select_category`, `ready_prompts_page`, `couple_page` оставлены
алиасами навигатора — иначе отвалились бы клавиатуры, уже висящие у людей в переписке;
(4) `ORDER BY sort_order, id` вместо голого `sort_order`: у всех восьми парных категорий
`sort_order = 0`, и порядок был физическим порядком строк, то есть произвольным;
(5) наследование `prompt_gender` через `Path()` запрашивается только для вложенных узлов —
у корней наследовать не от кого, и горячий путь «Фото для себя» не получил лишнего запроса.

2026-08-07 — Этап 4: Вёрстка, тексты тарифов и «Мой профиль» — tag `stage-4-done` — коммиты
`939d223` и `9df83bc`, миграция 00028, контейнер пересоздан 09:01:38 UTC. Заказчик прогнал
живой прогон в ВК и принял. Нетривиальные решения:
(1) листалка держит обе кнопки одним рядом под списком — первая версия разносила «Вперёд»
наверх по букве ТЗ, но это давало 7 строк при лимите ВК в 6, и `fitInlineRows` склеивал
контент обратно в две колонки; заказчик независимо попросил свести кнопки в один ряд;
(2) `fitInlineRows` теперь склеивает строки снизу вверх — внизу навигация, её не жалко,
наверху контент по одной кнопке в ряд;
(3) кнопка выхода на списках названа «🏠 В меню», иначе рядом оказывались два «Назад»;
(4) переименования кнопок не доезжают сами (label из БД перебивает код), поэтому миграция
сбрасывает `keyboard = NULL` у затронутых экранов, и `EnsureDefaults` поднимает раскладку
из определений — проверено запросом к боевой БД, все четыре раскладки перестроились;
(5) заодно закрыт баг с пустым текстом `bottom_menu` — 521 отказ ВК с error-100 за пять
дней; после релиза за сутки таких ошибок 0.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `ROADMAP.md` отмечены `[x]` и реально проверены.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит» (`git rev-parse --short HEAD`), «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `ROADMAP.md` и спецификации в `docs/`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
