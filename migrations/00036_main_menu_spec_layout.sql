-- +goose Up
-- +goose StatementBegin

-- Главное меню приводится к схеме из п. 1 ТЗ:
--   1. Фото для себя
--   2. Фото по своему промту
--   3. Изменить фото | Парное/семейное фото
--   4. Детские фото | Тренды
--   5. Поздравления | Мое фото
--
-- Сохранённая в БД раскладка перебивает кодовую (см. этап 4), поэтому её надо
-- сбросить — EnsureDefaults поднимет новую из определения экрана. Перед этой
-- миграцией сохранённая раскладка сверена с кодовой: подписи, цвета и
-- видимость совпадали, то есть правок из админки в ней нет и сброс ничего
-- не теряет.
--
-- Заодно возвращается кнопка «Мое фото», убранная из меню в этапе 4. Вход
-- через «Мой профиль» остаётся, кнопка ведёт в тот же раздел; если решение
-- этапа 4 в силе, кнопку достаточно скрыть в редакторе клавиатуры.

UPDATE messages SET keyboard = NULL, updated_at = now() WHERE key = 'main_menu';

-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin

UPDATE messages SET keyboard = NULL, updated_at = now() WHERE key = 'main_menu';

-- +goose StatementEnd
