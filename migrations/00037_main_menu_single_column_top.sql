-- +goose Up
-- +goose StatementBegin

-- Уточнение раскладки главного меню под схему п. 1 ТЗ.
--
-- В таблице ТЗ третья строка разделена косой чертой, а четвёртая и пятая —
-- вертикальной. Это разные вещи: «Изменить фото» и «Парное/семейное фото»
-- стоят каждая своей строкой, а парами идут только новые разделы. В миграции
-- 00036 обе кнопки ошибочно свели в один ряд. Итоговая раскладка:
--   1. Фото для себя
--   2. Фото по своему промту
--   3. Изменить фото
--   4. Парное/семейное фото
--   5. Детские фото | Тренды
--   6. Поздравления | Мое фото
--
-- Шесть строк — ровно лимит inline-клавиатуры ВК. Седьмой строки не будет:
-- следующая кнопка сможет встать только второй в существующий ряд.
--
-- Раскладка в БД перебивает кодовую, поэтому сбрасываем её под пересборку.

UPDATE messages SET keyboard = NULL, updated_at = now() WHERE key = 'main_menu';

-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin

UPDATE messages SET keyboard = NULL, updated_at = now() WHERE key = 'main_menu';

-- +goose StatementEnd
